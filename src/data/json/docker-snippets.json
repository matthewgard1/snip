{
  "default": [
    {
      "id": "docker-prune-all",
      "title": "Docker System Prune",
      "description": "Remove all unused containers, networks, images, and volumes",
      "language": "bash",
      "code": "docker system prune -a --volumes"
    },
    {
      "id": "docker-inspect-ip",
      "title": "Docker Container IP",
      "description": "Get IP address of a running container",
      "language": "bash",
      "code": "docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' container_name"
    }
  ]
}